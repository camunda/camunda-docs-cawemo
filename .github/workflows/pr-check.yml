name: development

on:
  pull_request:
    branches: [master]

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        -
      - name: Detect unmerged deployment changes
        run: make check-unmerged-deployment-changes